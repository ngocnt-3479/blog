<div class="row" id="user-profile">
  <aside class="col-md-3">
    <section class="user_info">
        <%= render "shared/user_info" %>
      </section>
  </aside>
  <div class="col-md-8">
    <% if @posts.any? %>
      <h3>Potsts (<%= @user.posts.count %>)</h3>
      <%= render @posts %>
      <%== pagy_bootstrap_nav @pagy %>
    <% end %>
  </div>
</div>
